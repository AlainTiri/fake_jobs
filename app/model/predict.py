from joblib import load


class Model_SVM:
    # def __init__(self):
    #     filename = '/models/model_svm.joblib'
    #     with open(filename, 'rb') as f:
    #         # load(f)
    #         self.model = load(f)

    def predict_svm(self, to_predict):
        # r = self.model.predict(to_predict)
        return "prediction SVM"
